\documentclass[12pt]{article}
\usepackage{amsfonts, amssymb, amsmath}

% set the indentation to zero pixel for the entire document
\parindent0px

\begin{document}

The distributive property states that $ a(b+c) = ab + ac$ for all $ a, b, c \in \mathbb{R}$.\\[6pt]
% you can specify the space between each line after a soft return by adding [50pt] for example for 50px between line
The set $A$ is defined to be $ \{1, 2, 3, 4\} $.\\[6pt]
The movie ticket cost around $ \$12.50 $.

\[ 2 (\frac{ x-2}{x-2}) \]

\include{note}

\end{document}